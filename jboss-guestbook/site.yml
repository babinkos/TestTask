- hosts: 127.0.0.1
  tasks:
  - name: Write node host key to known hosts
    connection: local
    command: "ssh-keyscan -H {{ inventory_hostname }} >> ~/.ssh/known_hosts"

  debug:
    msg: "ssh-keyscan -H {{ inventory_hostname }}"
      
- hosts: all
  name: Include all .json and .jsn files in vars/all and all nested directories (2.3)
  vars_files:
      - group_vars/all/params.json

- hosts: web-servers
  name: configure and deploy the webservers and application code
  roles:
      - jboss
#     - guestbookapp

#- name: deploy MySQL and configure the databases
#  hosts: db-servers
# remote_user: root

#  roles:
#    - mysql-gb-db       